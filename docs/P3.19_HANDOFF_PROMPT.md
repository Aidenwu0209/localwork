# DejaView P3.19 继续执行交接 Prompt（2026-08-04）

把下面整段作为新 Agent 的第一条指令。仓库当前任务是 **P3.19 doing**，
不是 accept；不得把本交接点误报为成熟产品最终验收。

---

你是 DejaView（全本地数字记忆体）的继续执行工程师。

- 仓库：`github.com/Aidenwu0209/localwork`
- 本地：`/Users/wu/Projects/Aidenwu0209/localwork/`
- 比赛：AMD AI DevMaster Hackathon Track 2 · Agentic AI
- 截止：2026-08-06 23:59 UTC+8
- 状态机唯一源：`TASKBOARD.json`

## 必读顺序

1. `docs/EXECUTION_HANDBOOK.md`，先读 §12，再按需读 §0–§4 / §8 / §9 / §10
2. `STATUS.md`
3. `TASKBOARD.json`（只继续 P3.19；P3.1–P3.18 不重做）
4. 本文件
5. `docs/verification-log.md` 最后两个章节
6. `docs/superpowers/specs/2026-08-03-final-contest-polish-design.md`
7. `docs/superpowers/plans/2026-08-03-final-contest-polish.md`
8. `deploy/server/DEPLOY.md`、`docs/benchmarks.md` §2、`docs/licenses.md`

## 已完成且可复用

1. 默认本地隐私/产品栈的严格受管生命周期已经在提交
   `f1d2b8041e99ab86a1643c8d68b54faff9287bc5` 完成并推送。不要重写；只做
   P3.19 要求的当前实机复验。
2. 新增 `make submission-check` 和 `scripts/submission_check.py`：
   - 当前共 33 个定向安全回归；Windows 为 29 PASS + 4 个 POSIX-only skip，
     Linux/macOS 必须重新跑满；
   - 当前真实提交包 19/19 通过；
   - 检查时长、MP4 容器、H.264/AAC/1080p、SHA、连续 SRT、六幕顺序、
     DOCX/PPTX 结构、README 链接、Git 跟踪密钥文件名、发布文本/OOXML
     隐私与人工提交边界；
   - 已修复祖先 symlink、Git 失败 fallback、ACT 10 子串、非规范 ZIP
     member、不支持压缩导致崩溃、伪装 MP4、README 注释/代码隐藏、OOXML
     entity 编码凭据等绕过。
3. 新英文主旁白提交视频已经生成并应保留：
   - MP4：`docs/assets/demo/dejaview-p34-six-act-20260802-en-3m.mp4`
   - SRT：`docs/assets/demo/dejaview-p34-six-act-20260802-en-3m.srt`
   - 构建器：`scripts/build_submission_video.py`
   - 195.2 秒，1920×1080，30 fps，H.264/AAC，SHA-256
     `99c2542a45f81e0b184453fa456bd5e4846fbc2d9c2eb4ea3e5d8ed9c08c1216`
   - SRT SHA-256
     `4901b8c0320af22b8df086e6a2ec3d3f221cc35d3e35ae1224c4f16831f86df9`
   - 英文旁白综合响度 -16.1 LUFS、峰值 -1.5 dB；15 个关键帧已人工检查，
     无黑屏、裁切或加载态。
   - 原始验收视频
     `docs/assets/demo/dejaview-p34-six-act-20260802.mp4` 及其 SHA 不得改。
4. README、README.zh、视频 manifest、Markdown 规格和 DOCX 已同步 3:15.2
   视频。DOCX 已重新渲染为完整 8 页并逐页检查，无孤立第 9 页；可访问性
   审计为 high/medium/low 均 0。
5. 当前工作树已经跑过一次完整 `make test`：release 85、agentd 101、
   memoryd 82+6 subtests、capture 22、浏览器 9、Mac 20、server 46 全部通过；
   仅有既有 FastAPI/TestClient 弃用警告。此结果不是干净检出或 GitHub CI
   证据。
6. PPTX 已按模板原位同步并完成 QA：
   - 第 7 页 `release-limitations` 已改为 `3:15 English-primary demo`；
   - 备注保留 2:37 原始证据不变，并说明 3:15.2 英文提交版合规；
   - 仅 `slide7.xml` 与 `notesSlide7.xml` 改变；7 页均已渲染检查，无溢出。
7. Actions 已固定为官方 Node 24 SHA：checkout v7.0.1 与 setup-uv v8.3.2；
   release contract 会拒绝恢复到浮动 `@vN`。
8. 隔离 UI 审计已完成 1440×900 / 390×844、axe 4.12.1 零 violation、无横向
   溢出、citation/evidence、Escape 关闭与焦点恢复。证据留在未跟踪的
   `.superpowers/p319-ui-audit/`，不得提交。
9. Windows capture 与控制入口已加入并在本机实跑：
   - `clients/capture` 使用 Win32 窗口枚举 + `mss` 内存截图，pyobjc 仅 Darwin；
   - `deploy/windows/dejaview.cmd` 提供 `doctor`、受管 `radeon-cloud` 隧道、
     `product-up/status/down` 与 `capture`，PID/创建时间/命令行和端口归属均
     fail-closed；capture 测试 23/23，真实前台 PNG 1090x760 解码通过。

## 尚未完成（按顺序继续）

1. **授权 Mac/Radeon 环境的当前产品实机复验**：
   - 只终止精确确认属于本仓库的旧 8090/8101 进程；禁止 broad kill；
   - `make product-up` → `make product-status`，要求当前源码、进程归属、健康
     schema、Sentinel/Radeon 路由都严格 READY；
   - 用隔离合成数据完成 Radeon recall → citation → evidence，验证隐私、
     Honcho/profile、断链与 Local Metal fallback；
   - 本 Windows 主机没有 Docker/Compose、本地 Sentinel 模型栈，且
     `radeon-cloud` 的本机公钥未获远端授权；Windows 入口会在本地 Sentinel
     缺失或隧道认证失败时安全停止。不得把隔离 mock UI 审计代替真实受管
     Radeon/Honcho/fallback 门禁。
2. **CI 与干净检出**：
   - Actions 固定 SHA 已改完；在 POSIX 主机补 shell/JS/格式检查、完整
     `make test` 与无 skip 的 submission-check；
   - 从精确暂存树做全新 clone/checkout，至少复验两次幂等 setup/测试；
   - 推送后等待 Linux `offline-contract` 和 macOS `macos-capture` 全绿且无
     可控警告。
3. **最终状态**：只有上述证据全部成立，才把 `TASKBOARD.json` 的 P3.19
   `doing → accept`，在 `docs/verification-log.md` 写 `[VERIFY]`，同一提交推送。

## 开工命令（先只读）

```bash
cd /Users/wu/Projects/Aidenwu0209/localwork
git pull --ff-only
git status -sb
git log -1 --format='%H%n%an <%ae>%n%B'
python3 -m unittest tests.release.test_submission_check -v
python3 scripts/submission_check.py
```

预期：33 个 submission 单测在 POSIX 主机全部通过，真实提交包 19/19；若不一致，先定位差异，
不要改 manifest 迎合损坏文件。

## 定死的产品与工程纪律

- 产品是数字记忆体，不做普通 RAG；叙事和五模型分层不改。
- Mac/Windows 是数据主权端，AMD 服务器是无状态算力端；只用逻辑名
  `brain/perceive/sentinel/fast/embed`。
- thinking fast-track 必须 `enable_thinking=false`。
- Honcho 固定 `340175ad`；只改 `deploy/mac/honcho-patches/`，永远不要
  `git add third_party/honcho`。
- 不读取、提交或打印 `.env`、密钥、真实 PII、临时公网主机/实例信息；服务器
  只通过本机授权别名 `ssh radeon-cloud`。
- GPU 前先 `rocm-smi`；共租时 brain 用 Q6_K、先停 perceive、MTP 默认关，
  不碰未知 KFD 进程，不 OOM Dolphin。
- commit 作者只能 `Aidenwu0209 <1418557225@qq.com>`；禁止
  `Co-authored-by`、`Generated-with` 或任何 AI trailer。每次提交后检查
  `git log -1 --format='%B'`，有 Cursor trailer 就用 `commit-tree` 重写。
- 不提交 `.superpowers/`、临时渲染目录或 dirty `third_party/honcho`。
- AMD 注册、Rules、官方仓库 fork/英文 PR、仓库公开、最终平台上传和最后服务
  器演示数据检查始终是人工 gate，仓库测试不得声称已完成。

---
